# Changelog Commit `34bc976`

Generated: `2026-06-04T18:57:07`

Commit: `34bc976` (`feat: add import workflows and refresh app surfaces`)

# Objective

Capture the accumulated Argent application work after `f98acbf`, including data import support, goal reserved accounts, transaction details, mobile/navigation updates, the UDS squircle refresh, expanded tests, migrations, public assets, and supporting documentation.

# Changes

## 1. Import And Data Portability

### Objective:

Add a user-scoped import path that can preview or commit JSON/CSV data while preserving the existing export workflow.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>app/api/import/route.ts</code></td>
  <td>Adds multipart import handling with dry-run previews, validation, duplicate detection, user scoping, and transactional writes.</td>
</tr>
<tr>
  <td><code>lib/data-import.ts</code></td>
  <td>Adds shared import schemas, normalizers, file parsing, import keys, summaries, and warnings/errors for backup and current export formats.</td>
</tr>
<tr>
  <td><code>components/import-dialog.tsx</code></td>
  <td>Adds the client import dialog with file selection, preview results, final import, cache invalidation, and localized copy.</td>
</tr>
<tr>
  <td><code>app/api/export/route.ts</code>, <code>components/export-dialog.tsx</code></td>
  <td>Extends data portability surfaces around the new import/export flow.</td>
</tr>
<tr>
  <td><code>lib/data-import.test.mjs</code></td>
  <td>Adds import parser and normalization coverage.</td>
</tr>
</table>

## 2. Goal Reserved Accounts

### Objective:

Represent financial goal progress through one-to-one virtual reserved accounts and expose that data consistently through the app.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>prisma/schema.prisma</code></td>
  <td>Adds <code>FinancialGoalAccount</code>, links goals/users to reserved accounts, and relaxes the Salt Edge customer uniqueness constraint.</td>
</tr>
<tr>
  <td><code>prisma/migrations/*</code></td>
  <td>Adds migrations for the initialized schema, goal reserved accounts, and Salt Edge customer uniqueness change.</td>
</tr>
<tr>
  <td><code>lib/goal-account.ts</code></td>
  <td>Adds helpers for goal account naming, transfer references, amount formatting, and goal serialization.</td>
</tr>
<tr>
  <td><code>app/api/goals/*</code>, <code>app/(main)/Goals/page.tsx</code></td>
  <td>Updates goal creation, editing, display, and progress handling around linked reserved accounts.</td>
</tr>
<tr>
  <td><code>components/dashboard/*</code></td>
  <td>Feeds goal account data into dashboard analytics, priorities, support, and overview modules.</td>
</tr>
</table>

## 3. UDS, Squircles, And Shared UI

### Objective:

Refresh the application visual system around UDS surfaces, continuous-corner rendering, denser app chrome, and consistent shared primitives.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>lib/UDS.ts</code>, <code>lib/PRISM.ts</code></td>
  <td>Renames the shared visual system from PRISM to UDS and expands shared surfaces, controls, dividers, hover states, and selection styles.</td>
</tr>
<tr>
  <td><code>components/squircle-provider.tsx</code>, <code>app/globals.css</code></td>
  <td>Adds measured squircle support, CSS shape tokens, sheet variables, and refreshed global surface styling.</td>
</tr>
<tr>
  <td><code>components/ui/*</code>, <code>components/form-dialog.tsx</code></td>
  <td>Refreshes shared primitives, dialogs, sheets, dropdowns, inputs, tables, tabs, tooltips, buttons, cards, and form dialog step controls.</td>
</tr>
<tr>
  <td><code>components/mobile-dock.tsx</code>, <code>components/app-sidebar.tsx</code>, <code>components/nav-*.tsx</code></td>
  <td>Adds mobile navigation and updates sidebar/navigation surfaces.</td>
</tr>
<tr>
  <td><code>public/*</code>, <code>app/logo-demo/page.tsx</code></td>
  <td>Refreshes app icons, favicon assets, SVG logo templates, and the public logo demo route.</td>
</tr>
</table>

## 4. Finance Workflows And App Surfaces

### Objective:

Carry the refreshed UX and data model through core finance workflows and operational pages.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>app/(main)/Transactions/[id]/page.tsx</code></td>
  <td>Adds a dedicated transaction detail route with account, counterparty, tag, source, and record metadata.</td>
</tr>
<tr>
  <td><code>app/(main)/*</code>, <code>components/*-table.tsx</code></td>
  <td>Updates Accounts, Bills, Budgets, Calendar, Goals, Notifications, Spreadsheets, Transactions, and dashboard pages around the refreshed UI.</td>
</tr>
<tr>
  <td><code>components/bills-table.tsx</code>, <code>components/budget-table.tsx</code>, <code>components/transactions-table.tsx</code></td>
  <td>Refreshes finance tables, row actions, detail links, and responsive presentation.</td>
</tr>
<tr>
  <td><code>components/spreadsheet-workspace.tsx</code>, <code>hooks/use-spreadsheet.tsx</code></td>
  <td>Updates spreadsheet chrome, headers, formula bar, selection styling, tabs, and supporting hooks.</td>
</tr>
<tr>
  <td><code>lib/chart.ts</code>, <code>hooks/use-chart-*.ts</code></td>
  <td>Consolidates chart constants, types, and helpers into a shared chart module and updates consumers.</td>
</tr>
</table>

## 5. Auth, Admin, Tests, And Docs

### Objective:

Round out the product refresh with safer account flows, admin styling, broader tests, and documentation updates.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>app/(auth)/*</code>, <code>components/auth/*</code>, <code>lib/password.ts</code></td>
  <td>Refreshes auth screens, adds the auth noise background, updates password behavior, and adds password tests.</td>
</tr>
<tr>
  <td><code>app/(admin)/*</code>, <code>components/admin/*</code></td>
  <td>Updates admin dashboard, user, account, audit, notification, bill, budget, goal, and settings screens for the refreshed surface system.</td>
</tr>
<tr>
  <td><code>lib/data-access.ts</code>, <code>app/api/**</code></td>
  <td>Updates permissions, API scoping, OAuth routes, bank connection routes, notification checks, and transaction APIs.</td>
</tr>
<tr>
  <td><code>package.json</code>, <code>pnpm-lock.yaml</code></td>
  <td>Adds the Geist dependency and extends the test script to run password, import, auth, and dashboard coverage.</td>
</tr>
<tr>
  <td><code>docs/*</code>, <code>DESIGN.md</code>, <code>.agents/research/*</code></td>
  <td>Updates setup, schema, technical, visual identity, squircle, design, and market/trademark research documentation.</td>
</tr>
</table>
