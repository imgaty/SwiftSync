# Changelog Commit `370d3ad`

Generated: `2026-05-30T21:32:43`

Commit: `370d3ad` (`feat: redesign dashboard and harden auth`)

# Objective

Capture the accumulated application work since `1fc66e3` (`Commit sidebar component.`), including the dashboard command-center redesign, auth hardening, PRISM UI refresh, route cleanup, generated-file cleanup, and supporting tests/documentation.

# Changes

## 1. Dashboard Command Center

### Objective:

Replace the previous single-page dashboard implementation with a modular dashboard system that can compose analytics, overview, recent activity, financial focus, and priority review surfaces.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>components/dashboard/*</code></td>
  <td>Adds dashboard-specific primitives, data hooks, priority ranking, module layout, analytics, overview, recent activity, financial focus, and support sidebar components.</td>
</tr>
<tr>
  <td><code>app/(main)/page.tsx</code></td>
  <td>Rebuilds the dashboard around the new module grid, account filter, export action, and priority brief drawer.</td>
</tr>
<tr>
  <td><code>app/(main)/Dashboard/page.tsx</code></td>
  <td>Routes <code>/Dashboard</code> through the main dashboard implementation.</td>
</tr>
<tr>
  <td><code>components/section-cards.tsx</code></td>
  <td>Aligns summary cards with the shared dashboard metric card treatment.</td>
</tr>
<tr>
  <td><code>components/dashboard/*.test.mjs</code></td>
  <td>Adds dashboard priority and style regression coverage.</td>
</tr>
</table>

## 2. PRISM UI System And Surface Styling

### Objective:

Consolidate the visual system around PRISM surfaces, flatter dashboard cards, spotlight interaction styling, and consistent button/card primitives.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>lib/PRISM.ts</code></td>
  <td>Expands PRISM with shared card surfaces, glass overlays, glide highlight styles, hover states, and flat/elevated shadows.</td>
</tr>
<tr>
  <td><code>app/globals.css</code></td>
  <td>Adds spotlight surfaces, PRISM glass styling, dashboard sidebar scroll masking, and related interaction styling.</td>
</tr>
<tr>
  <td><code>components/ui/button.tsx</code></td>
  <td>Refreshes button variants around PRISM surfaces, focus rings, disabled states, and icon sizing.</td>
</tr>
<tr>
  <td><code>components/ui/card.tsx</code></td>
  <td>Moves the reusable card primitive to the shared PRISM card surface and spotlight boundary.</td>
</tr>
<tr>
  <td><code>components/ui/*</code></td>
  <td>Updates core primitives including dialogs, sheets, dropdowns, inputs, tables, context menus, selects, tooltips, tabs, and skeletons.</td>
</tr>
<tr>
  <td><code>components/ui/glide-highlight.tsx</code>, <code>components/ui/tab-switcher.tsx</code></td>
  <td>Adds new interaction primitives used by dashboard and navigation surfaces.</td>
</tr>
</table>

## 3. Authentication And Session Hardening

### Objective:

Tighten session validity, 2FA behavior, redirect safety, and account recovery flows.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>prisma/schema.prisma</code></td>
  <td>Adds <code>sessionVersion</code> and <code>emailVerified</code> fields to <code>User</code>.</td>
</tr>
<tr>
  <td><code>prisma/migrations/20260529120000_session_version_email_verified</code></td>
  <td>Adds the database migration for the new user auth fields.</td>
</tr>
<tr>
  <td><code>lib/session.ts</code>, <code>lib/auth-helpers.ts</code></td>
  <td>Adds session-version matching so older session tokens can be invalidated after account-sensitive changes.</td>
</tr>
<tr>
  <td><code>lib/auth-backup-codes.ts</code>, <code>lib/auth-pending-2fa.ts</code>, <code>lib/auth-redirect.ts</code></td>
  <td>Adds backup-code consumption, bounded pending 2FA session helpers, and safe redirect utilities.</td>
</tr>
<tr>
  <td><code>app/api/auth/[action]/route.ts</code></td>
  <td>Updates login, 2FA, registration, profile, password reset, and session flows around the new helpers and stronger validation.</td>
</tr>
<tr>
  <td><code>app/(auth)/*</code></td>
  <td>Refreshes auth screens and login 2FA UX, including backup-code entry and safer post-login routing.</td>
</tr>
<tr>
  <td><code>lib/auth-security.test.mjs</code></td>
  <td>Adds direct tests for redirect safety, backup-code normalization/consumption, and pending 2FA replacement behavior.</td>
</tr>
</table>

## 4. API, Data, And Finance Workflows

### Objective:

Carry the auth, validation, type, and UI updates through the finance routes and page workflows.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>app/api/**/route.ts</code></td>
  <td>Updates API route modules with shared headers, auth handling, validation, and formatting conventions.</td>
</tr>
<tr>
  <td><code>app/(main)/*/page.tsx</code></td>
  <td>Refreshes Accounts, Bills, Budgets, Calendar, Goals, Notifications, Spreadsheets, Transactions, and Settings page layouts.</td>
</tr>
<tr>
  <td><code>components/*-table.tsx</code>, <code>components/*-dialog.tsx</code></td>
  <td>Updates finance tables, dialogs, filters, tag pickers, export flows, and related interaction components.</td>
</tr>
<tr>
  <td><code>hooks/*</code>, <code>lib/*</code></td>
  <td>Updates shared finance, chart, spreadsheet, auth, bank, notification, and query utilities.</td>
</tr>
</table>

## 5. Navigation, Layout, And Sidebar Behavior

### Objective:

Simplify the app shell and stabilize sidebar interactions after the sidebar component work in the previous commit.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>components/ui/sidebar.tsx</code></td>
  <td>Restores the sidebar rail as a purpose-built transparent button with full-height edge interaction and cursor-anchored tooltips.</td>
</tr>
<tr>
  <td><code>components/app-sidebar.tsx</code>, <code>components/nav-pages.tsx</code>, <code>components/nav-user.tsx</code></td>
  <td>Updates navigation item styling, active states, user menu behavior, and page preferences.</td>
</tr>
<tr>
  <td><code>components/page-framework.tsx</code>, <code>app/(main)/layout.tsx</code></td>
  <td>Adjusts shell sizing, overflow behavior, page headers, and desktop/mobile layout constraints.</td>
</tr>
<tr>
  <td><code>.agents/research/2026-05-30-bug-sidebar-expand.md</code></td>
  <td>Documents the sidebar rail bug hunt, root cause, fix, and verification notes.</td>
</tr>
</table>

## 6. Cleanup And Dependency Changes

### Objective:

Remove stale generated files, demos, dead UI components, unused scripts, and static boilerplate assets from the tracked tree.

### Changes:

<table>
<tr>
  <th>Area</th>
  <th>Change</th>
</tr>
<tr>
  <td><code>.gitignore</code></td>
  <td>Adds local tooling ignores and keeps Prisma generated output ignored.</td>
</tr>
<tr>
  <td><code>lib/generated/prisma/*</code></td>
  <td>Removes generated Prisma client files from version control; they are regenerated during build.</td>
</tr>
<tr>
  <td><code>package.json</code>, <code>pnpm-lock.yaml</code></td>
  <td>Updates scripts, dependency set, overrides, and package lock state.</td>
</tr>
<tr>
  <td>Removed components and demo assets</td>
  <td>Deletes obsolete data tables, nav wrappers, drawer/progress/toggle primitives, old demo pages, generated fake-data scripts, and default public assets.</td>
</tr>
<tr>
  <td><code>FILE_CHECKLIST.md</code>, <code>docs/*</code>, <code>public/lang/*.json</code></td>
  <td>Updates repository checklist, documentation, and localized strings for the new flows.</td>
</tr>
</table>

# Verification

<table>
<tr>
  <th>Check</th>
  <th>Status</th>
</tr>
<tr>
  <td><code>pnpm run typecheck</code></td>
  <td>Passed.</td>
</tr>
<tr>
  <td><code>pnpm run test</code></td>
  <td>Passed.</td>
</tr>
</table>

# Summary

<table>
<tr>
  <th>Category</th>
  <th>Summary</th>
</tr>
<tr>
  <td>Dashboard</td>
  <td>New modular dashboard command center with overview, analytics, activity, focus, priority brief, and tests.</td>
</tr>
<tr>
  <td>Security</td>
  <td>Session-version invalidation, email verification field, safer redirects, backup-code support, pending 2FA handling, and auth tests.</td>
</tr>
<tr>
  <td>UI</td>
  <td>PRISM surfaces, spotlight styling, refreshed primitives, navigation updates, and sidebar rail fix.</td>
</tr>
<tr>
  <td>Cleanup</td>
  <td>Generated Prisma client removed from tracking, stale assets/scripts/components deleted, dependencies refreshed.</td>
</tr>
</table>
